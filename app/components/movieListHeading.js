export default function MovieListHeading ({ heading }) {
    return (
      <div>
        <h1 className="text-6xl p-8 font-bold text-center font-sans text-slate-100">{heading}</h1>
      </div>
    );
  }